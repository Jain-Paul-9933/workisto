# --- Core web ---
Django>=5.0,<5.2
djangorestframework>=3.15
django-cors-headers>=4.4        # so the Next.js frontends can call the API

# --- Database driver (PostGIS uses the same psycopg driver) ---
psycopg[binary]>=3.2

# --- Real-time (chat / websockets) — Channels ---
channels>=4.1
channels-redis>=4.2
daphne>=4.1                      # ASGI server that speaks HTTP + WebSocket

# --- Async jobs (pricing recompute, reminders, expiring PENDING bookings) ---
celery>=5.4
redis>=5.0

# --- Tests (your resume claims pytest — we'll actually use it) ---
pytest>=8.3
pytest-django>=4.9

# Added in later increments, noted here so the roadmap is visible:
#   stripe            -> payments increment
#   fastapi, uvicorn  -> high-throughput booking-ingest service
