# --- Core web ---
Django>=5.0,<5.2
djangorestframework>=3.15
django-cors-headers>=4.4        # so the Next.js frontends can call the API

# --- Database driver (PostGIS uses the same psycopg driver) ---
psycopg[binary]>=3.2

# --- Real-time (chat / websockets) — Channels ---
channels>=4.1
channels-redis>=4.2
daphne>=4.1                      # ASGI server that speaks HTTP + WebSocket

# --- Async jobs (pricing recompute, reminders, expiring PENDING bookings) ---
celery>=5.4
redis>=5.0

# --- Payments ---
stripe>=10.0                    # imported lazily; only needed when PAYMENT_GATEWAY=stripe
PyJWT>=2.9                      # short-lived tokens for the search-service boundary

# --- Search service (separate FastAPI app; same Postgres, no ORM) ---
fastapi>=0.115
uvicorn[standard]>=0.30
asyncpg>=0.30                   # async Postgres driver for the read hot-path
httpx>=0.27                     # FastAPI TestClient transport

# --- Tests (your resume claims pytest — we'll actually use it) ---
pytest>=8.3
pytest-django>=4.9
pytest-asyncio>=0.24            # async tests for Channels + the FastAPI service
